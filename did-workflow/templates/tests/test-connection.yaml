apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "did-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "did-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "did-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
