apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "did-website.fullname" . }}-test-connection"
  labels:
    {{- include "did-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "did-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
